package smalln

// Package variables : confifg2
var (
	cmdName   = "smalln" // the app name - should also be the workflow name
	shortDesc = "a small workflow."
)

func init() {
	// var registry = make(map[string]phase.PhaseFunc2)
	// phase.RegisterSingleFunc(registry, "vm.CheckVmSshAccess", vm.CheckVmSshAccess)
	// phase.RegisterSingleFunc(registry, "luc.DeployLuc", luc.DeployLuc)
	// phase.RegisterSingleFunc(registry, "dnfapt.UpgradeVmOs", dnfapt.UpgradeVmOs)

}
