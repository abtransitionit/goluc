name: os
description: test upgrading and updating a linux OS
phases:
  UpgradeOs:
    description: Upgrade a linux OS native repositories and packages to the latest version
    node: node.all
    fn: UpgradeOs

  WaitIsOnline:
    description: Wait a node to be online after a possible reboot
    node: node.all
    fn: WaitIsOnline
    dependency:
      - UpgradeOs
